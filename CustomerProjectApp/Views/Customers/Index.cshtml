@model IEnumerable<CustomerProjectApp.Models.Customer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.numberOfTables)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.smokers)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.numberOfTables)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.smokers)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdOfTable">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdOfTable">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdOfTable">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
